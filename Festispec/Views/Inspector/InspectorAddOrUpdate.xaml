<Page x:Class="Festispec.Views.Inspector.InspectorAddOrUpdate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="InspectorAddOrUpdate"
        DataContext="{Binding Source={StaticResource Locator}, Path=InspectorAddOrUpdate}">
    <StackPanel>
        <Label>Gebruikersnaam</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Username}" />
        <Label>Voornaam</Label>
        <TextBox Text="{Binding Path=EntityViewModel.FirstName}" />
        <Label>Achternaam</Label>
        <TextBox Text="{Binding Path=EntityViewModel.LastName}" />
        <Label>Email</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Email}" />
        <Label>IBAN</Label>
        <TextBox Text="{Binding Path=EntityViewModel.IBAN}" />
        <Label>Manager</Label>
        <ComboBox ItemsSource="{Binding Managers}"
                  DisplayMemberPath="Username"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding EntityViewModel.Manager_Id}"></ComboBox>
        <Label>Password</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Password}" />
        <Label>Telefoonnummer</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Telephone}" />
        <Label>In dienst genomen op</Label>
        <DatePicker SelectedDate="{Binding Path=EntityViewModel.HiredFrom}"/>
        <Label>Uit dienst gegaan</Label>
        <DatePicker SelectedDate="{Binding Path=EntityViewModel.HiredTo}"/>
        <Label>Certificatie startdatum</Label>
        <DatePicker SelectedDate="{Binding Path=EntityViewModel.CertificationFrom}"/>
        <Label>Certificatie einddatum</Label>
        <DatePicker SelectedDate="{Binding Path=EntityViewModel.CertificationTo}"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label>Postcode</Label>
                <TextBox Text="{Binding Path=EntityViewModel.PostalCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="6" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label>Huisnummer</Label>
                <TextBox Text="{Binding Path=EntityViewModel.HouseNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <Button Grid.Column="2" Command="{Binding SearchAddressCommand}">Zoek adres</Button>
        </Grid>

        <Label>Straat</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Street}" IsEnabled="false" />
        <Label>Stad</Label>
        <TextBox Text="{Binding Path=EntityViewModel.City}" IsEnabled="false" />
        <Label>Gemeente</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Municipality}" IsEnabled="false" />
        <Label>Land</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Country}" IsEnabled="false" />
        <Grid Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Margin="5px 10px" Command="{Binding NavigateBackCommand}" />
            <Button Grid.Column="1"  Content="Save" Margin="5px 10px" Command="{Binding SaveEntityCommand}"/>
        </Grid>
    </StackPanel>
</Page>