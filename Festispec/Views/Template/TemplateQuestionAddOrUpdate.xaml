<Page x:Class="Festispec.Views.Template.TemplateQuestionAddOrUpdate"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="TemplateQuestionAddOrUpdate"
      DataContext="{Binding Source={StaticResource Locator}, Path=TemplateQuestionAddOrUpdate}">

    <StackPanel>
        <Label>Vraag</Label>
        <TextBox Text="{Binding Path=EntityViewModel.Name}" />
        <Label>Vraag type</Label>
        <ComboBox ItemsSource="{Binding Path=QuestionTypes}"
                  DisplayMemberPath="Type"
                  SelectedValuePath="Type"
                  SelectedValue="{Binding Path=EntityViewModel.QuestionType_Type}" />

        <Grid Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Cancel" Margin="5px 10px" Command="{Binding CancelEntityCommand}" />
            <Button Grid.Column="1" Content="Save" Margin="5px 10px" Command="{Binding SaveEntityCommand}" />
        </Grid>
    </StackPanel>
</Page>