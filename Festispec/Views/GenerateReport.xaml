<Page x:Class="Festispec.Views.GenerateReport"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Festispec.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="GenerateReport" DataContext="{Binding GetGenerateReport, Source={StaticResource Locator}}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock FontSize="20">Genereer rapport</TextBlock>
        <Label Grid.Row="1">Selecteer periode</Label>
        <ListBox ItemsSource="{Binding OptionList}" SelectedItem="{Binding SelectedAmount}" Grid.Row="2"></ListBox>
        <Button Command="{Binding Path=DownloadCommand}" Grid.Row="3">Download PDF</Button>
    </Grid>
</Page>
